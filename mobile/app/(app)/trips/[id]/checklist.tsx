import { View, Text } from 'react-native';
export default function ChecklistScreen() {
  return <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}><Text>Checkliste (kommt in Task 11)</Text></View>;
}
